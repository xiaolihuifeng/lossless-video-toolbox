[pytest]
minversion = 8.0
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: fast unit tests without I/O
    integration: tests exercising real external processes or files
    gui: Qt widget tests requiring a display or offscreen platform
